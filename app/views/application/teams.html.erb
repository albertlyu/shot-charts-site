<h3><strong>All NCAA Men's Basketball Teams</strong></h3>
<div class="btn-group">
  <% @teams.each do |team| %>
  <a class="btn btn-default" style="width:220px; margin:5px" href="teams/<%= team.id %>"><%= team.team_name %> <%= team.team_mascot %> (<%= team.team_div %>)</a>
  <% end %>
</div>
